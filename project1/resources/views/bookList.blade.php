<x-layout>

    <x-slot:heading>
        Book List
    </x-slot:heading>

    {{-- @foreach ($books as $book)
        <h3 class="h3">{{ $book->title }}</h3>
        <p>{{ $book->description }}</p>
        <h3 class="h3">{{ $book->price }}</h3>
        <h3 class="h3">{{ $book->stock }}</h3>
    @endforeach --}}
</x-layout>
